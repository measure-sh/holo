# Boot / Device Selection

- Run when msh command is called
- Check for connected devices
- If no devices connected:
  - error our with prompt
- If 1 device connected:
 - Go into the main app
- If >1 devices connected:
  - Ask for which device to select in a list which can be navigated using keyboard and selected by enter
